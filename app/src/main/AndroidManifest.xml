<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network access for AutoEQ catalog/profile fetch (HTTPS only via networkSecurityConfig). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Audio routing. MODIFY_AUDIO_SETTINGS is normal-protection on all SDK levels. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- NOTE: 미디어 읽기 권한(디버그 직접재생 전용)은 src/debug/AndroidManifest.xml 로
         분리됨. 정식(release) 경로는 SAF(OpenDocument)라 미디어 권한이 필요 없다. -->

    <!-- Bluetooth address visibility for device-keyed AutoEQ selection (Phase 5).
         Android 12+ runtime permission; on lower SDKs the permission is implicit. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_CONNECT"
        android:minSdkVersion="31" />

    <!-- Foreground media playback service (lock-screen/notification controls, background playback).
         FOREGROUND_SERVICE_MEDIA_PLAYBACK required on API 34+; POST_NOTIFICATIONS on API 33+. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".AuralTuneApplication"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AuralTune"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AuralTune">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Media playback service: owns the ExoPlayer + MediaSession (lock-screen/notification
             transport, media buttons, background playback). UI connects via MediaController. -->
        <service
            android:name=".audio.AuralTuneMediaService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>
    </application>

</manifest>
