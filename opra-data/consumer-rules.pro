# OPRA data module consumer ProGuard rules.
# kotlinx.serialization @Serializable models are kept via the serialization plugin's
# own rules; add OPRA-specific keep rules here if release minification is enabled later.
