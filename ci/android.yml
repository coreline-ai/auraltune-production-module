# Archived CI pointer.
#
# The active GitHub Actions workflow is:
#   ../.github/workflows/android.yml
#
# This file is intentionally not a second workflow source of truth. Older
# versions of the project copied a workflow from ci/android.yml into a nested
# android-app checkout; that layout is no longer current for this repository.
#
# Local release readiness gates are run from:
#   ../tools/release_readiness.ps1

active_workflow: ../.github/workflows/android.yml
local_release_gate: ../tools/release_readiness.ps1
status: pointer_only
